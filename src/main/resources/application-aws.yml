cloud:
  aws:
    credentials:
      accessKey: "${AWS_ACCESS_KEY_ID}"
      secretKey: "${AWS_SECRET_ACCESS_KEY}"
    s3:
      bucket: honeybadgerdogfile
    region:
      static: us-east-2
    stack:
      auto: false